{
  "name": "@axionvera/benchmarks",
  "version": "0.1.0",
  "description": "Performance benchmarks for Axionvera SDK",
  "private": true,
  "scripts": {
    "benchmark": "node xdr-parsing.benchmark.js",
    "benchmark:ci": "node xdr-parsing.benchmark.js --ci"
  },
  "dependencies": {
    "@axionvera/core": "file:../packages/core",
    "@stellar/stellar-sdk": "^14.6.1"
  },
  "devDependencies": {
    "benchmark": "^2.1.4"
  }
}
